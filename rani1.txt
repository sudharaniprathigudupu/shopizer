pipeline {
    agent any
    triggers { cron('30 17 * * *') }
    stages {
        stage ('clone') {
            steps {
                git url: 'https://github.com/sudharaniprathigudupu/shopizer.git',
                 branch: 'release'
            }
        }
        stage ('build') {
            steps {
                sh 'mvn clean install'
            }
        }
    }
}    