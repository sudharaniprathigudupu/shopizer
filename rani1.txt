pipeline {
    agent any
    triggers { pollSCM('* * * * *') }
    stages {
        stage ('clone') {
            steps {
                git url: 'https://github.com/sudharaniprathigudupu/shopizer.git',
                 branch: 'devloper'
            }
        }
        stage ('build') {
            steps {
                sh 'mvn clean install'
            }
        }
    }
    }